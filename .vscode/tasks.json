{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "go build -o bin/go-getting-started.exe -v"
        },
        {
            "label": "launch",
            "type": "shell",
            "command": "heroku local web -f Procfile.windows"
        }
    ]
}